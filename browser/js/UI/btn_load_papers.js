var $ = require('jquery');
module.exports = $('#btn-load-papers').click(function(){
  // var selected_model = global.model_stats_display.selected_model();
  // var year;
  // if(selected_model.name) year = Number(selected_model.name.match(/\d{4}/));
  // else year = global.UI_year_select.year();
  // global.topic_document_viewer.year(year).data_type(global.document_viewer.PAPER)
  // .load().then(function(data){
  //   global.topic_document_viewer.data(data).update();
  // });
});
